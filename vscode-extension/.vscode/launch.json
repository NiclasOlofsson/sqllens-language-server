{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Run extension (test workspace)",
			"type": "extensionHost",
			"request": "launch",
			"args": ["--extensionDevelopmentPath=${workspaceFolder}", "${workspaceFolder}/../temp_auto/localtest"],
			"outFiles": ["${workspaceFolder}/dist/**/*.js"],
			"preLaunchTask": "npm: build"
		}
	]
}
